[pytest]
# Put the repo root on Python's import path so tests can `import mnist`.
# Without this, pytest only puts the tests/ folder on the path and every
# `from mnist.data import ...` fails with ModuleNotFoundError.
pythonpath = .
testpaths = tests
